{% extends 'base.html.twig' %}

{% block title %}Ajouter une adresse{% endblock %}

{% block content %}
    <a href="{{ path('account') }}" class="btn btn-light btn-sm">Retour</a>
    <hr>
    <h1 class="sous-titre" style="text-align: center">Gérer mes adresses</h1>
    <div class="loginh">
    {{ form_start(form) }}
        <div class="row">
            <div class="col-md-12">{{ form_row(form.name) }}</div>
            <div class="col-md-6">{{ form_row(form.address) }}</div>
            <div class="col-md-6">{{ form_row(form.postal) }}</div>
            <div class="col-md-6">{{ form_row(form.ville) }}</div>
            <div class="col-md-6">{{ form_row(form.phone) }}</div>
        </div>
    {{ form_end(form) }}
    </div>
{% endblock %}
