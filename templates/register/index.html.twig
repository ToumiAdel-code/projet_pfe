{% extends 'base.html.twig' %}

{% block content %}
    <div class="loginh">
        <h1 style="text-align: center; color: #c72026">Inscription</h1>
        {% if notification %}
            <div class="alert alert-info">{{ notification }}</div><hr>
        {% endif %}
        {{ form(form) }}
    </div>
{% endblock %}