{% extends 'base.html.twig' %}


{% block content %}
    <div class="row">
        <div class="col-md-3">
            <h2 class="sous-titre"><img src="{{ asset('assets/img/loupe.png') }}" height="30px"> Rechercher</h2>
            {{form(form)}}
        </div>
        <div class="col-md-1"></div>
        <div class="col-md-8">
            <h2 class="sous-titre"> <img src="{{ asset('assets/img/boutique.png') }}" height="40px" style="margin-right: 12px">Nos produits</h2>

            <div class="row product-container">
            {% for product in products %}
                <div class="col-md-4">
                    {% include 'product/single_product.html.twig' %}
                </div>
            {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
